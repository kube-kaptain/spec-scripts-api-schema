name: Spec Check Filter Release

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    uses: kube-kaptain/buildon-github-actions/.github/workflows/spec-check-filter-release.yaml@fix-gh-release
    permissions:
      contents: write
      packages: write
      checks: write
    with:
      spec-type: schema
      spec-validation-type: python3-jsonschema
